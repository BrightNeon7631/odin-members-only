<header>
    <a class="header-logo" href="/">Members Only</a>
    <div class="header-links">
        <% if (locals.user) {%>
            <a href="/logout">Logout</a>
          <% } else { %>
            <a href="/sign-up">Sign Up</a>
            <a href="/login">Login</a>
          <%}%>
    </div>
</header>